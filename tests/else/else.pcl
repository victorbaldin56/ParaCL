fst = 0;
snd = 1;
iters = ?;
if (!iters) {
        print 0;
}
else {
        iters = iters - 1;
        while (iters > 0) {
                tmp = fst;
                fst = snd;
                snd = snd + tmp;
                iters = iters -1;
        }
        print snd;
}